{
  "name": "claude-context-size-guard",
  "description": "Stops you from sending a prompt into an already-bloated context. Measures live context size on every prompt and, past a configurable token threshold (default 75,000), answers with a one-line notice telling you to run /compact instead of burning a full turn.",
  "author": {
    "name": "hacknitive",
    "url": "https://github.com/hacknitive"
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/src/hooks/context-size-guard.js\"",
            "timeout": 10,
            "statusMessage": "Checking context size..."
          }
        ]
      }
    ]
  }
}
